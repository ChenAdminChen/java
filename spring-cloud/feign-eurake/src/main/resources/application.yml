server:
  port: 8765
#  servlet:
#    context-path: /client
spring:
  application:
    name: service-feign

# 注册中心的地址
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

#开启feign自带的断熔处理
feign:
  hystrix:
    enabled: true